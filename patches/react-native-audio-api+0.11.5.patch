diff --git a/node_modules/react-native-audio-api/ios/audioapi/ios/AudioAPIModule.mm b/node_modules/react-native-audio-api/ios/audioapi/ios/AudioAPIModule.mm
index 5ace868..3e666cf 100644
--- a/node_modules/react-native-audio-api/ios/audioapi/ios/AudioAPIModule.mm
+++ b/node_modules/react-native-audio-api/ios/audioapi/ios/AudioAPIModule.mm
@@ -122,7 +122,9 @@ RCT_EXPORT_METHOD(
     setAudioSessionActivity : (BOOL)enabled resolve : (RCTPromiseResolveBlock)
         resolve reject : (RCTPromiseRejectBlock)reject)
 {
-  dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
+  // AVAudioSession must be configured/activated on the main thread; background queue works on the
+  // simulator but often fails on device (setActive/configure errors with empty NSError).
+  dispatch_async(dispatch_get_main_queue(), ^{
     NSError *error = nil;
 
     auto success = [self.audioSessionManager setActive:enabled error:&error];
@@ -131,7 +133,7 @@ RCT_EXPORT_METHOD(
       NSDictionary *meta = @{
         @"nativeCode" : @(error.code),
         @"nativeDomain" : error.domain ?: @"",
-        @"nativeDesc" : error.description ?: @"",
+        @"nativeDesc" : error.localizedDescription ?: error.description ?: @"",
       };
 
       NSError *jsError =
diff --git a/node_modules/react-native-audio-api/ios/audioapi/ios/system/AudioSessionManager.mm b/node_modules/react-native-audio-api/ios/audioapi/ios/system/AudioSessionManager.mm
index d8c4430..3b13ca1 100644
--- a/node_modules/react-native-audio-api/ios/audioapi/ios/system/AudioSessionManager.mm
+++ b/node_modules/react-native-audio-api/ios/audioapi/ios/system/AudioSessionManager.mm
@@ -1,6 +1,10 @@
 #import <AVFAudio/AVFAudio.h>
 #import <audioapi/ios/system/AudioSessionManager.h>
 
+@interface AudioSessionManager ()
+- (bool)configureAudioSession:(NSError **)outError allowDeactivateRetry:(BOOL)allowDeactivateRetry;
+@end
+
 @implementation AudioSessionManager
 
 static AudioSessionManager *_sharedInstance = nil;
@@ -42,7 +46,7 @@ static AudioSessionManager *_sharedInstance = nil;
       self.audioSession.allowHapticsAndSystemSoundsDuringRecording == self.allowHapticsAndSounds);
 }
 
-- (bool)configureAudioSession
+- (bool)configureAudioSession:(NSError **)outError allowDeactivateRetry:(BOOL)allowDeactivateRetry
 {
   NSError *error = nil;
 
@@ -55,17 +59,37 @@ static AudioSessionManager *_sharedInstance = nil;
                          options:self.desiredOptions
                            error:&error];
 
+  // When re-activating after markInactive (our isActive is NO but OS session may still be active),
+  // setCategory often fails until we deactivate. Only do this from setActive:YES — not from
+  // setAudioSessionOptions while we still consider the session active, or we'd leave audio off.
+  if (error != nil && allowDeactivateRetry) {
+    NSLog(@"[AudioSessionManager] setCategory failed (%@), deactivating and retrying", error);
+    NSError *deactError = nil;
+    [self.audioSession setActive:NO error:&deactError];
+    if (deactError != nil) {
+      NSLog(@"[AudioSessionManager] setActive(NO) before category retry: %@", deactError);
+    }
+    error = nil;
+    [self.audioSession setCategory:self.desiredCategory
+                              mode:self.desiredMode
+                           options:self.desiredOptions
+                             error:&error];
+  }
+
   if (error != nil) {
-    NSLog(@"Error while configuring audio session: %@", [error debugDescription]);
+    NSLog(@"[AudioSessionManager] Error while configuring audio session: %@", [error debugDescription]);
+    if (outError != NULL) {
+      *outError = error;
+    }
     return false;
-  } else {
-    NSLog(
-        @"[AudioSessionManager] Configured audio session: category=%@, mode=%@, options=%lu",
-        self.audioSession.category,
-        self.audioSession.mode,
-        (unsigned long)self.audioSession.categoryOptions);
   }
 
+  NSLog(
+      @"[AudioSessionManager] Configured audio session: category=%@, mode=%@, options=%lu",
+      self.audioSession.category,
+      self.audioSession.mode,
+      (unsigned long)self.audioSession.categoryOptions);
+
   if (@available(iOS 13.0, *)) {
     if (self.audioSession.allowHapticsAndSystemSoundsDuringRecording !=
         self.allowHapticsAndSounds) {
@@ -76,6 +100,9 @@ static AudioSessionManager *_sharedInstance = nil;
         NSLog(
             @"Error while setting allowHapticsAndSystemSoundsDuringRecording: %@",
             [error debugDescription]);
+        if (outError != NULL) {
+          *outError = error;
+        }
         return false;
       }
     }
@@ -105,7 +132,8 @@ static AudioSessionManager *_sharedInstance = nil;
   self.allowHapticsAndSounds = allowHaptics;
 
   if (configChanged && self.isActive) {
-    [self configureAudioSession];
+    NSError *cfgErr = nil;
+    [self configureAudioSession:&cfgErr allowDeactivateRetry:NO];
   }
 }
 
@@ -122,7 +150,7 @@ static AudioSessionManager *_sharedInstance = nil;
   }
 
   if (active) {
-    success = [self configureAudioSession];
+    success = [self configureAudioSession:error allowDeactivateRetry:YES];
 
     if (!success) {
       return false;
